{
    "name": "recipe-worker",
    "version": "1.0.0",
    "description": "Cloudflare Worker for Recipe App",
    "main": "src/index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "deploy": "wrangler deploy"
    },
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "wrangler": "^4.70.0"
    },
    "dependencies": {
        "jspdf": "4.2.0"
    }
}
